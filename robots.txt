# Allow all search engines to crawl all pages
User-agent: *
Allow: /
Disallow:

# Sitemap location
Sitemap: https://uniimize.in/sitemap.xml

# Crawl-delay (optional - removes if causing issues)
# Crawl-delay: 1
